{
  "name": "acmetest",
  "version": "1.0.0",
  "scripts": {
    "flow": "flow",
    "build": "babel src/ -d dist/",
    "start": "npm run flow && npm run build && node ./dist/server/start.js",
    "test": "npm run flow && npm run build && mocha dist/test/*"
  },
  "dependencies": {
    "body-parser": "^1.18.3",
    "express": "4.16.2",
    "jsonschema": "^1.2.4"
  },
  "devDependencies": {
    "@babel/cli": "^7.2.3",
    "@babel/core": "^7.2.2",
    "@babel/preset-env": "^7.3.1",
    "@babel/preset-flow": "^7.0.0",
    "chai": "^4.2.0",
    "chai-http": "^4.2.1",
    "flow-bin": "0.91.0",
    "mocha": "^5.2.0"
  }
}
